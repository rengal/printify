todo
-EplRenderer.GetEncodingCodePage - fix
-remove protocols other than escpos/epl when adding printer
-debug commands - fix codepage

todo
- It might be worth moving BuildCommandDescription from src/Printify.Web/Mapping/DocumentMapper.cs into the ESC/POS command descriptors (see src/Printify.Infrastructure/Printing/EscPos/CommandDescriptors/ICommandDescriptor.cs) so command metadata lives with parsing.
- add hint when hovering over icons (stopped warning, toggle operatinops)
- workspace name must not be the same format as token (disallow names
like frozen-dragon-1234)

document view refactor
- Define view-oriented DTOs in Printify.Web.Contracts (base debug fields, element types incl. type=none).
- Implement server conversion to view DTOs and add new documents view endpoint.
- Add server-side tests for conversion + endpoint (auth, 404, empty, happy path).
- Update client to use view endpoint and render via absolute positioning with fallback.
- Run relevant tests and fix issues; update docs if needed.

completed
- Printers do not start automatically when the server starts.
- When open printer, it doesn't subscribe to  documents stream
- keep transparent background for images
- store relative path to media, not full local path
- make promts to clear/delete stylyzed, not standard msgbox
- use "esc/pos emulation" phrasing in ui, not "esc/pos". Same with epl zpl tspl
- add clear endpoint to clear documents and UI implementation
